services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: toplamlar-akademiyasi:latest
    container_name: toplamlar-akademiyasi
    ports:
      - "8080:80"
    restart: unless-stopped
